<h2 class="page-header">Recently Added</h2>
<div class="fish-row">
    <% fishes.forEach(fish => { %>
    <a class="fish-name" href="/fish/<%= fish.id %>">
        <div><%= fish.name %></div>
    </a>
    <% }) %>
</div>